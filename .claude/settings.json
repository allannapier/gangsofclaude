{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p .claude/game-state && cp .claude/game-state/save.json .claude/game-state/save.backup.json 2>/dev/null || true"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if the game server is running at http://localhost:3456/api/health. If it is, inform the user they can play at http://localhost:5174. If not, suggest running 'cd web && bun run server/index.ts' to start the server, then 'cd web/client && bun run dev' for the UI."
          }
        ]
      }
    ]
  }
}
